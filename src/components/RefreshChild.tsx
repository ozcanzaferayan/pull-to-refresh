import React from "react";
import { StyleSheet, Text, View } from "react-native";

const RefreshChild = (props) => {
  console.log("refreshchild", props);
  return (
    <View>
      <Text>RefreshChild</Text>
    </View>
  );
};

export default RefreshChild;

const styles = StyleSheet.create({});
